# Copy to .env and fill in for production use.

SECRET_KEY=change-this-to-a-long-random-string

# Leave unset to use the built-in SQLite database (zero setup).
# To use MySQL instead, uncomment and fill in:
# DATABASE_URL=mysql+pymysql://username:password@localhost/siddhivinayak_expense_manager
